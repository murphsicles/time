// Auto-converted from /tmp/df-time-0.3.47/time-0.3.47/src/sys/local_offset_at/wasm_js.rs

use num_conv::prelude::*;
use crate::convert::*;
use crate::{ OffsetDateTime, UtcOffset };
/// Obtain the system's UTC offset.
#[inline]
fn local_offset_at(datetime: OffsetDateTime) -> Option<UtcOffset> {
 let _ = datetime.into();
 let timezone_offset = (js_date.get_timezone_offset() as i32) * - Minute::per_t::<i32>(Hour);
 UtcOffset::from_whole_seconds(timezone_offset).ok()
}

